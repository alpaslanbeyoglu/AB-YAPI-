rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Admin & License rules
    match /licenses/{email} {
      allow read, write: if true;
    }

    // User data & project settings rules
    match /users/{userId}/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
